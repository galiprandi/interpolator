{
  "name": "interpolator",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "xlsx:demo": "pnpm tsx scripts/run-interpolation.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "packageManager": "pnpm@10.22.0"
}